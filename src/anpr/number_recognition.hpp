#ifndef PROTOSHOP_ANPR_NUMBER_RECOGNITION
#define PROTOSHOP_ANPR_NUMBER_RECOGNITION

#include <iostream>
#include "../image/b_w.hpp"

std::string numberRecognition(BWImage* img);
// detect the number written on binary image of plate
// without changing the image

#endif
